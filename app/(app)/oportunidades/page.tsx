import Link from "next/link";import { and,desc,eq,like,or } from "drizzle-orm";import { db } from "@/database/client";import { opportunities } from "@/database/schema";import { EmptyState } from "@/components/empty-state";import { requireUser } from "@/lib/auth/session";import { getFavoriteIds } from "@/lib/local-settings";import { formatBRL } from "@/lib/money";
export default async function OpportunitiesPage({searchParams}:{searchParams:Promise<{q?:string;status?:string}>}){const user=await requireUser(),filters=await searchParams,clauses=[eq(opportunities.userId,user.userId),eq(opportunities.deletedAt,null as unknown as string)],favorites=new Set(await getFavoriteIds(user.userId));if(filters.q)clauses.push(or(like(opportunities.title,`%${filters.q}%`),like(opportunities.category,`%${filters.q}%`),like(opportunities.brand,`%${filters.q}%`))!);if(filters.status)clauses.push(eq(opportunities.status,filters.status));const rows=await db.select().from(opportunities).where(and(...clauses)).orderBy(desc(opportunities.createdAt)).all();return <div><div className="mb-6 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-black">Oportunidades</h1><p className="text-sm text-[var(--muted)]">Favorite, compare e decida antes de comprometer seu caixa.</p></div><div className="flex gap-2"><Link className="button secondary" href="/captura">Captura em lote</Link><Link className="button" href="/oportunidades/nova">Nova oportunidade</Link></div></div><form className="panel mb-5 grid gap-3 p-3 sm:grid-cols-[1fr_180px_auto]"><input className="input" name="q" defaultValue={filters.q} placeholder="Buscar título, categoria ou marca"/><select name="status" defaultValue={filters.status}><option value="">Todos os status</option><option value="EM_ANALISE">Em análise</option><option value="NEGOCIACAO">Negociação</option><option value="COMPRADO">Comprado</option><option value="VENDIDO">Vendido</option><option value="DESCARTADO">Descartado</option></select><button className="button secondary">Filtrar</button></form>{!rows.length?<EmptyState title="Nenhuma oportunidade por aqui" text="Cadastre manualmente ou use a captura em lote." href="/oportunidades/nova" action="Cadastrar oportunidade"/>:<form action="/comparar" className="panel overflow-x-auto"><div className="flex items-center justify-between gap-3 border-b border-[var(--border)] p-3"><p className="text-xs text-[var(--muted)]">Marque de 2 a 3 itens</p><button className="button secondary py-2">Comparar selecionados</button></div><table><thead><tr><th>Comparar</th><th>Oportunidade</th><th>Preço pedido</th><th>Status</th><th>Origem</th><th>Capturada</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><input type="checkbox" name="id" value={row.id} aria-label={`Comparar ${row.title}`}/></td><td><Link className="font-bold hover:underline" href={`/oportunidades/${row.id}`}>{favorites.has(row.id)?"★ ":""}{row.title}</Link><p className="text-xs text-[var(--muted)]">{[row.brand,row.model,row.condition].filter(Boolean).join(" · ")}</p></td><td className="font-bold">{formatBRL(row.askingPriceCents)}</td><td><span className="badge">{row.status.replaceAll("_"," ")}</span></td><td>{row.source}</td><td>{new Date(row.capturedAt).toLocaleDateString("pt-BR")}</td></tr>)}</tbody></table></form>}</div>}
