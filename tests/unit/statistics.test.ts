import { describe,expect,it } from "vitest";
import { detectOutliers, marketStatistics } from "@/lib/scoring/statistics";
describe("estatística de comparáveis",()=>{const now=new Date("2026-09-01T12:00:00Z");it("calcula média, mediana e quartis",()=>{const stats=marketStatistics([100,120,140,160,180].map(priceCents=>({priceCents,collectedAt:"2026-08-25T12:00:00Z",included:true,priceType:"ANUNCIADO",condition:"USADO_BOM"})),now);expect(stats).toMatchObject({count:5,minCents:100,maxCents:180,meanCents:140,medianCents:140,q1Cents:120,q3Cents:160,confidence:"ALTA"})});it("não usa amostra desmarcada ou outlier",()=>{const stats=marketStatistics([{priceCents:100,collectedAt:"2026-08-25",included:true,priceType:"ANUNCIADO",condition:"USADO"},{priceCents:999,collectedAt:"2026-08-25",included:false,priceType:"ANUNCIADO",condition:"USADO"},{priceCents:888,collectedAt:"2026-08-25",included:true,outlier:true,priceType:"ANUNCIADO",condition:"USADO"}],now);expect(stats.count).toBe(1);expect(stats.medianCents).toBe(100)});it("sinaliza outlier pelo IQR sem apagar",()=>expect(detectOutliers([100,102,103,104,400])).toEqual([false,false,false,false,true]))});
